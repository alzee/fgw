search on proj page is still global, need to be limited in type

proj list page js need to be re-design

导出报表

sql in ajax.php

date pick bug

js md5

锁定日期
建设阶段细化

admin modify/add/delete project
admin delete user?

the lock alert shouldn't show out if this is not my proj

每个单位可以生成自己的进度月报表

should I change investment,invest_mon,invest_plan,sum_year to int(11)?

cron to 'update projects set alert=2'

append upload ui to right place
js limit size
js limit type
php limit size
php limit type

omit(clip) long text in table td with ellipsis, mousecover shows 

create view in db

IE not support xhr type 'json'
